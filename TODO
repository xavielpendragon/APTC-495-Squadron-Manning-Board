#TODO

Incorporate a method of when Adding personnel having a drop down to assign them to a section.

Find a method for having qualifications tied to each position. "The system must automatically enforce qualification-based logic to prevent uncertified personnel from being assigned to restricted slots" - 5.2.2 Stimulus A / 5.2.3 Req-4

Have these values preloaded into the project so when you run a new from scratch they're preloaded.

Future - Expand the import.csv file to include an almost complete squadron.

Future - Brainstorming idea to split the script.js file into multiple files to cut down on the long code digging. Helps on adding/removing features on the fly.

Future - Figure out a method of "securing" the PII when exporting to a csv / and, maybe, have an unsecured export so that you can ensure there's a base that can be uploaded.